name: view_image
description: >
  Analyse an image and answer a question about it using a free vision-capable
  LLM (Google Gemini Flash preferred, OpenRouter free tier as fallback).
  Pass a local file path or a public URL.
  Use this tool whenever the user shares an image and asks what is in it,
  or when a task requires understanding image content.
toolset: hub
destructive: false
requires: [uv]
env_required: [GOOGLE_AI_API_KEY, OPENROUTER_API_KEY]
schema:
  type: object
  properties:
    source:
      type: string
      description: >
        Absolute local file path (e.g. /tmp/photo.jpg) or public image URL
        (https://…). Supported formats: JPEG, PNG, WEBP, GIF.
    question:
      type: string
      description: >
        What to ask about the image, e.g. "What objects are in this image?"
        Defaults to a general description if omitted.
    sender:
      type: string
      description: >
        Name or username of the person who sent the image (e.g. LINE display name).
        Used for logging — recorded as @sender in ~/.garudust/view_image.log.
  required: [source]
model: gemini-flash-latest
fallback_model: nvidia/nemotron-nano-12b-v2-vl:free
command: uv run --with httpx ./run.py {source} {question} {sender}
