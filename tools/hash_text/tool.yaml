name: hash_text
description: Compute the SHA-256 hash of a string. Use when you need a checksum or a deduplication key for a piece of text.
toolset: hub
destructive: false
schema:
  type: object
  properties:
    text:
      type: string
      description: The string to hash
  required: [text]
command: printf '%s' {text} | shasum -a 256 | awk '{print $1}'
